/* Punto 1: Se requiere desarrollar un programa que utiliza un puntero a un
número entero, identificado como ptr. Defina el puntero y asignele una direccion
de memoria válida con la sintaxis de C*/
#include <stdio.h>
int main(){
    int valor;
    int *ptr;
    ptr = &valor;
    return 0;
}