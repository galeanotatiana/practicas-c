// Realizar un programa que permita, a través de una función ordenar un arreglo de 20 elementos. 
// Esta función recibirá dos parámetros : el primero indicará arreglo a ordenar, y el segundo
// indicará si el orden será “A” - Ascendente (de menor a mayor) o “D” Descendente (de mayor a menor)

#include <stdio.h>
void ordenar(int[], int, char[]);
void imprimir(int[], int);
void ordenar (int arreglo[], int N, char orden){
    int aux;
    int ordenado = 0;
}